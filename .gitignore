# Dependencies
node_modules/
package-lock.json

# Build output
dist/
build/

# IDE and editors
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Logs
logs/
*.log
npm-debug.log*

# Environment files
.env
.env.local
.env.*.local

# OS files
Thumbs.db
Desktop.ini

# Temporary files
*.tmp
*.temp
.cache/
# Kept local on purpose; narrow patterns, because core.ignorecase makes a broad one
# swallow any source file whose name happens to contain the word.
CLAUDE.md
*.db
*.db-journal

# Go CAM engine binaries (build with: cd server/cam-engine && .\build.ps1)
*.exe
server/cam-engine/dist/

# Deprecated code (can be deleted later)
src/_deprecated/

#example
/Example/*
.claude/settings.local.json
database.*
# Drawings, not the code that reads them: *DXF* used to hide dxf.go and dxf_test.go too.
/dxf/
*.dxf
*.DXF

.playwright-mcp/*

STL/*
Svg/*
certs/*
internal/handler/opcua_config.json
AGENTS.md
plotter_pen.db
